// create a form component with:
  // input + label of Name:
  // ** how to save the divs that make up the character?
    // ^ save each section (eyes, face, etc) into state?
  // submit button onClick saves to firebase

// eyes, nose, mouth, etc
  // each tab onClick loads array of selected part into the box.
  // show 2 eyes on MVP
  // ** how to make eye designs scroll left right?
    // ^ box displays index of [0] [1] [2] [3]
    // left arrow on click displays index of [0 - 1] [1 - 1] [2 - 1] [3 - 1]
    //right arrow on click displays index of [0 + 1] [1 + 1] [2 + 1] [3 + 1]
      // catch undefined index and display an empty box

// saved characters
  // on each character, set key = firebase unique keys
  // onClick, popup a confirmation notice: "are you sure you want to override your current character"
  // if yes, set creation character div key = clicked character's firebase unique key


/*
  // would eye, mouth, etc each be different components?
    // they will essentially be the same html but different css.
    // should i use js ot change the css or use react to give them a different class?
*/